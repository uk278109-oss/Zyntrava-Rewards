const {db,auth,send,fail}=require('./_lib');
module.exports=async(req,res)=>{if(req.method!=='POST')return send(res,405,{ok:false});try{const u=await auth(req);const b=req.body||{};const displayName=String(b.displayName||'Member').trim().slice(1,50)||'Member';const countryCode=String(b.countryCode||'PK').trim().slice(0,4).toUpperCase();await db().ref('users/'+u.uid).update({displayName,countryCode});const p=(await db().ref('users/'+u.uid).once('value')).val()||{};await db().ref('publicProfiles/'+u.uid).update({displayName,countryCode,earnedPoints:p.earnedPoints||0});send(res,200,{ok:true});}catch(e){fail(res,e)}};
