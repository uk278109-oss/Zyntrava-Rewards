const {admin,db,auth,requireAdmin,send,fail}=require('../_lib');
module.exports=async(req,res)=>{if(req.method!=='POST')return send(res,405,{ok:false});try{const u=await auth(req);requireAdmin(u);const b=req.body||{};if(!String(b.title||'').trim()||!Number(b.rewardPoints)||!String(b.instructions||'').trim())throw Object.assign(new Error('Title, reward and instructions are required'),{status:400});const ref=db().ref('tasks').push();await ref.set({id:ref.key,title:String(b.title).trim(),category:String(b.category||'General'),instructions:String(b.instructions).trim(),rewardPoints:Number(b.rewardPoints),countries:Array.isArray(b.countries)?b.countries:[],url:String(b.url||''),status:'active',createdBy:u.uid,createdAt:admin.database.ServerValue.TIMESTAMP});send(res,200,{ok:true,id:ref.key});}catch(e){fail(res,e)}};
